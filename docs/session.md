## Overview

This is the handoff document between working sessions on the thesis project — what has been completed, what is pending, and where the next session should pick up. It is not a deliverables checklist (that is `scope.md`) and not an architecture log (that is `decisions.md`). Read this first when resuming work; update it at the end of each session. Each session section records the date, what changed, and what is unresolved. Pending items carry forward until resolved. (Renamed from `state.md` in Session 11 — same document, same purpose, name now matches its actual job.)

## Table of Contents

- [Overview](#overview)
- [Submission State](#submission-state)
- [Session Log](#session-log)
  - [Session 1 — prior to 2026-07-02](#session-1-prior-to-2026-07-02)
  - [Session 2 — 2026-07-02](#session-2-2026-07-02)
  - [Session 3 — 2026-07-04 to 2026-07-05](#session-3-2026-07-04-to-2026-07-05)
  - [Session 4 — 2026-07-05](#session-4-2026-07-05)
  - [Session 5 — 2026-07-07](#session-5-2026-07-07)
  - [Session 6 — 2026-07-08](#session-6-2026-07-08)
  - [Session 7 — 2026-07-08 (Ch.2 alignment audit + patch)](#session-7-2026-07-08-ch2-alignment-audit-patch)
  - [Session 8 — 2026-07-08 (shipped to `main`)](#session-8-2026-07-08-shipped-to-main)
  - [Session 9 — 2026-07-08 (cross-chapter/vision/rubric audit + patch)](#session-9-2026-07-08-cross-chaptervisionrubric-audit-patch)
  - [Session 10 — 2026-07-08 (consolidated proposal audit + patch)](#session-10-2026-07-08-consolidated-proposal-audit-patch)
  - [Session 11 — 2026-07-10 (docs reorg, consolidated-proposal ship, Paged.js pipeline)](#session-11-2026-07-10-docs-reorg-consolidated-proposal-ship-pagedjs-pipeline)
  - [Session 12 — 2026-07-10 (CLI mode + GUI fallback: D19/D20)](#session-12-2026-07-10-cli-mode-gui-fallback-d19d20)
  - [Session 13 — 2026-07-10 (retire standalone Ch.2/Ch.3 drafts)](#session-13-2026-07-10-retire-standalone-ch2ch3-drafts)
  - [Session 14 — 2026-07-10 (retire Ch.1 + flat consolidated; footnote rendering resolved)](#session-14-2026-07-10-retire-ch1-flat-consolidated-footnote-rendering-resolved)
  - [Session 15 — 2026-07-12 (font-resolution bug fixed; anchor collisions and orphaned headings resolved)](#session-15-2026-07-12-font-resolution-bug-fixed-anchor-collisions-and-orphaned-headings-resolved)
  - [Session 16 — 2026-07-12 (Paged.js retired; endnotes/flat variant is the living document again)](#session-16-2026-07-12-pagedjs-retired-endnotesflat-variant-is-the-living-document-again)
  - [Session 17 — 2026-07-12 (prototype: M2 validated live; CLI mode redefined to include execution + confirmation gate)](#session-17-2026-07-12-prototype-m2-validated-live-cli-mode-redefined-to-include-execution-confirmation-gate)
  - [Session 18 — 2026-07-12 (prototype: M2 completed — reversibility classifier and execution wired up)](#session-18-2026-07-12-prototype-m2-completed-reversibility-classifier-and-execution-wired-up)
- [Pending Items](#pending-items)
- [Files to Know](#files-to-know)

## Submission State

| File | Chapter | Status | Last Changed |
|------|---------|--------|--------------|
| `research-methods/module 2/submissions/summative-assessment-1.pdf` | Ch. 1 Introduction, as submitted | **Session 14: HTML source retired** — frozen as the submitted record, matching the Ch.2/Ch.3 pattern from Session 13. No live source remains for this chapter; any future Ch.1 content changes happen in the consolidated document instead | 2026-07-10 |
| `research-methods/consolidated/SynapseOS_Proposal_Chapters_1_to_3.html` | Consolidated Ch.1–3 monograph — **the sole living document, as of Session 16** | **Session 14:** HTML source retired (frozen as historical snapshot) when the paged variant became the sole living document. **Session 16:** revived as the living document — the Paged.js same-page-footnote pipeline was retired (see `research-methods/archive/paged-footnotes-attempt/README.md` for the full reasoning: ~35% page-density loss from the polyfill's own layout imprecision, not just genuine footnote-space overhead). Restored from git history (the exact pre-retirement state), then given the same fixes already proven on the paged variant in Session 15 — 7 duplicate anchor `id`s namespaced, 2 genuinely orphaned headings fixed (here, native Chromium `page-break-after: avoid` works correctly, no workaround needed), all 37 TOC/LOT/LOF entries re-derived against the font-fixed 43-page PDF. **Session 17:** CLI mode's description (Table 3.2 paragraph + footnote 8) updated to match the same-day prototype decision to make CLI mode execute through a confirmation gate rather than just propose | 2026-07-12 |
| `research-methods/consolidated/SynapseOS_Proposal_Chapters_1_to_3.pdf` | Consolidated Ch.1–3 monograph (flat/endnotes variant) | **Session 16:** re-exported via the now-font-fixed `tools/export_pdf.py` — 43 pages (was 42 with the wrong font, before Session 15's fontconfig fix was applied here too), all TOC/LOT/LOF entries verified against `pdftotext` output | 2026-07-12 |

---

## Session Log

### Session 1 — prior to 2026-07-02

- Established SynapseOS architecture and wrote Chapter 1 (SA1) and Chapter 3/Methodology (SA3.1) — *labeled Chapter 2 at the time; renumbered to Chapter 3 in Session 5*
- Made key architecture decisions D1–D9 (see `decisions.md`)
- Revised Table 3.1 OS column to "SynapseOS" (Debian 13 as substrate)
- Removed OS row from Table 3.2; folded Debian 13 into caption
- Added OSWorld "how it works" sentence to Section 1.1
- Applied footnote system to SA1 (7 footnotes) and SA3.1 (7 footnotes)
- Switched study interface from TUI to GUI mode (D11) — updated Table 3.1, paragraph after Table 3.2, Section 3.5
- Recorded D10 (conversation memory model) and D11 (GUI study mode) in `decisions.md`
- Updated `scope.md` with GUI mode hardware spec and Final Thesis Compilation section
- Copied writing guidelines PDF to `research-methods/module 3/references/writing-guidelines.pdf`

### Session 2 — 2026-07-02

Applied Mapúa ITRD writing guidelines to both HTML submission files:

**CSS changes (both files):**
- Right margin corrected: 1.25 in → 1.0 in (left stays at 1.25 in)
- Line spacing: 1.5 → 2.0 (double space per guidelines)
- Paragraph indent: 1.27 cm → 1.50 cm
- Removed 0.8 em bottom paragraph margin (no extra space between paragraphs)
- First paragraphs after headings: now indented (guidelines indent all paragraphs)
- Chapter number label: bold, `text-transform: uppercase` removed
- Chapter title: `font-weight: bold` added (was missing)
- `h2` (second-level headings): title case instead of all-caps; `letter-spacing` removed; top margin 30 pt
- `h3` (third-level): top margin 30 pt
- Lists: 1.50 cm left indent, no inter-item gap
- References heading: left-aligned (was centered)
- Reference entries: 12 pt (was 11 pt)

**SA3.1 additionally:**
- `h4` (fourth-level): regular weight, no italic (guidelines: fourth-level = regular font style)
- Table/figure captions: non-italic, 12 pt
- **Table 3.1.**, **Table 3.2.**, **Figure 3.1.** label numbers wrapped in `<strong>`

**Commits:** `docs(decisions)` · `style(ch1)` · `style(methodology)`

### Session 3 — 2026-07-04 to 2026-07-05

- Recorded **D12** (Debian 13 + XFCE/X11 as the invisible GUI-mode substrate; bare Debian for TUI mode) and **D13** (post-thesis "Overlay" product mode — hotkey/systray-summoned agent over a visible desktop) in `decisions.md`
- Created `layers.md` — OS-layer reference guide (stack diagram, "where SynapseOS sits," TUI/GUI/Overlay mode table, distro-identity checklist)
- Updated `vision.md`, `stack.md`, `future-features.md` to reflect D12/D13
- Created `prototype/` scaffold: `go.mod`, `cmd/synapse/main.go` (M2 walking-skeleton — sample task suite, Ollama call at temperature 0, markdown-fence stripping), `internal/ollama/client.go` (REST client: `Generate`, `Ping`), `README.md`, `setup.md`, `build-order.md` (M2–M9 milestones). **Not yet run against a live Ollama instance — open empirical risk.**
- Full drift audit of both submitted chapters against current decisions (D3, D7, D9, D10, D12) and correction of all found drift:
  - SA1 (Ch. 1): "userland" → "session layer" (11 places), footnote 3 rewritten, Android analogy replaced, RQ2/Objective 2 rewritten to the actual NL-to-bash pipeline, RQ3/Objective 4 rewritten to the expert-baseline design (D9), GUI-scope-creep language removed from RQ1/Objective 1/Significance, Limitations names Qwen2.5-Coder-3B-Instruct as committed, "Linux distribution" self-description removed, orphaned MCP/AT-SPI footnotes deleted and footnotes renumbered 1–5 (verified)
  - SA3.1 (Ch. 3/Methodology): "userland"/"Linux distribution" fixed, Table 3.1 + Platform-specificity limitation changed from "Wayland compositor" to "XFCE (X11 session)" per D12, D10's memory model added verbatim to Section 2.1a. Footnotes unchanged (still 1–7, verified)
  - Full detail in `scope.md`'s Final Thesis Compilation section
- Regenerated both PDFs from the corrected HTML via headless Chromium (Playwright), resolved fully in user space (no root): downloaded Chromium's ~32 shared libs + Liberation/DejaVu fonts via `apt-get download` + `dpkg -x` + `LD_LIBRARY_PATH`. Verified new page/char counts against git history of the originals (SA1: 11→12 pages, 18,545→18,551 chars; SA3.1: 24→28 pages, 37,685→38,661 chars — both consistent with the actual text added)
- Deleted stale `.txt` mirrors (`git rm`) — HTML is now sole source of truth for both chapters
- Updated `scope.md` Final Thesis Compilation checklist with `[x]` completions and dates
- Shipped as 5 concern-scoped commits (see `git log`): `docs(decisions)`, `fix(ch1)`, `fix(methodology)`, `chore(submissions)`, `docs(scope)`

### Session 4 — 2026-07-05

**Planning docs:**
- Added a **Critical Path** section to `scope.md` — build track vs. ethics track dependency graph gating the pilot study; flagged the ethics application package as the actual bottleneck since IRB turnaround is the least controllable variable
- Created `roadmap.md` — thin cross-horizon (H0/H1/H2) status index, pointers only. Extracted the operational "Status" column out of `vision.md`'s Horizons table so `vision.md` stays purely about *why*, not *where things stand*

**Study design changes:**
- **D9 revised**: Windows and Linux now share one dual-boot machine (native performance both ways, no VM confound) instead of two separate machines; macOS stays dedicated (unreliable Linux support on Apple Silicon, and macOS's license blocks virtualizing macOS itself on non-Apple hardware). Study now needs **3 physical machines, not 4**. Updated `decisions.md`, `scope.md` Infrastructure section, and SA3.1 Table 3.1 + System Environment paragraph.
- **D14 added**: novice/power-user classification now requires a 3-task behavioral screener (file organization, process monitoring, application management) confirming self-reported proficiency, not self-report alone — motivated by the failure mode where general technical skill (e.g., software development) doesn't guarantee fluency with a given OS's native tools. Self-report/behavioral mismatches are excluded from the study.
- **D15 added**: prior AI/LLM exposure upgraded from a binary yes/no screening item to a graded frequency scale + tool identity; tracked as an exploratory covariate (same treatment as the OS-type subgroup), not a third grouping variable — n=20 is already split 10/10 by novice/power user, a further split would leave cells too small for inference.
- **Open, not yet decided**: whether to add an explicit *minimum* per-OS recruitment quota to D5/`scope.md`, to guarantee the realized sample can never end up all-Windows (or all one OS). Recommended against increasing n to properly *power* OS-specific subgroup claims (would need ~50+ participants, and isn't what the core within-subjects hypothesis needs) — recommended instead just a small quota floor per OS. **User was deciding when the session ended — pick this up first next time.**

**SLM choice reaudit (web search):**
- Confirmed Qwen2.5-Coder-3B-Instruct is still the best sub-4B choice as of 2026-07-05. Checked and rejected: Qwen3-Coder-Next (MoE, 80B total params — blows the CPU-only memory budget regardless of benchmark quality), Phi-4-mini (right size/license, but no NL2Bash-specific evidence), SmolLM3-3B (beats the wrong baseline — general Qwen2.5-3B, not the code-specialized Coder variant), LiteCoder-Terminal benchmark (doesn't test anything under 4B)
- **Resolved the long-blocked NDSS LAST-X 2026 citation**: authors are Jacobs, Lapon, Naessens (DistriNet, KU Leuven). The paper's own per-model accuracy table remains unextracted (binary/FlateDecode PDF, no available tool gets past it) — citable as corroborating literature only, not for its specific ranking
- Findings folded into `decisions.md` D3, `brainstorm.md`, `llm-selection-research.txt`, and SA3.1 Section 2.1a (added reference [25], added direct comparison numbers against Llama 3.2 variants, added a reconfirmation paragraph)

**SA3.1 chapter-quality fixes:**
- Section 2.1a tightened: was 7 dense paragraphs repeating the same "58%, best sub-7B" claim three times; cut to 6, the Westenfelder-methodology deep-dive reduced to one sentence pointing at Section 2.3
- Fixed a real inconsistency: Section 2.3's "Intent parsing accuracy" bullet described a *different* evaluation methodology (parsed-intent-vs-annotation matching) than 2.1a (execution-based scoring) — both now describe the same Westenfelder et al. methodology consistently
- Section 2 and Section 3 given orienting intro paragraphs matching Section 1's existing style (state the substance directly, no "this section covers..." framing) — fixes the unexplained jump from system-design content (2.1) to study-procedure content (2.2–2.5)
- Section 1.2a: added an explicit explanation of why only SynapseOS (not Condition B) is OSWorld-benchmarked — OSWorld evaluates autonomous agents, Condition B is human-operated

**TOC + PDF outline/bookmarks, both chapters:**
- SA3.1 got a Table of Contents, List of Tables, and List of Figures (Mapúa writing-guidelines Section F/G); SA1 got a Table of Contents only (no tables/figures in that chapter)
- Both have real page numbers baked into the HTML (two-pass: export → locate each heading's actual page via `pdftotext` → fill in → re-export) and a genuine PDF bookmarks sidebar, via Chrome DevTools Protocol's `generateDocumentOutline` (which Chromium's plain print-to-PDF CLI flag doesn't expose — needed the full tooling below to get at it)
- **Caveat, tracked in `scope.md`'s Final Thesis Compilation section:** the in-page TOC page numbers are hardcoded, not self-updating (Chromium doesn't support live `target-counter()` cross-references) — any future edit that shifts pagination needs the numbers re-derived by hand. The PDF outline itself *does* regenerate correctly on every export automatically.

**Built self-contained PDF export tooling (`tools/`),** replacing an ad-hoc bash/apt-get pipeline and a broken hand-rolled CDP/WebSocket client:
- `tools/venv/` — Python venv, `pip install playwright` (only real Python dependency)
- `tools/vendor/` — vendored Chromium binary + ~98 shared libs + `poppler-utils` + fonts, all resolved via `apt-get download`/`dpkg -x` (no root, no system installs), gitignored
- `tools/export_pdf.py` — drives the vendored Chromium via Playwright; calls `Page.printToPDF` directly over CDP for `generateDocumentOutline` (Playwright's stable API doesn't expose it yet); also has a `find_page()` helper for TOC page-number lookups
- `tools/requirements.txt`, `tools/TOOLING.md` (inventory of what's Python vs. vendored-native vs. genuinely irreducible — Python itself, the kernel/dynamic-linker/glibc, architecture/distro compatibility), `Makefile` (`make setup`, `make export FILE=... [OUT=...]`)

**Repo hygiene:**
- Ran `/gitignore` — regenerated `.gitignore` from the gitignore.io API for the detected stack (Go via `prototype/go.mod`, Python via `tools/requirements.txt`, plus macOS/Linux/Windows/VS Code baselines), merged into a managed block, all prior custom entries preserved above it. No already-tracked files conflicted.

**Consistency audit (background fork):** read all of SA3.1 against `scope.md` line by line. Found and fixed 8 gaps — most importantly, the novice/power-user subgroup + condition×group interaction (the study's central D5 design feature) was completely missing from the Statistical analysis script item, and the GUI-mode session manager (literally what Condition A is in the study) had no Go Runtime checklist line at all, only the TUI did. Also added: error-type categorization, the error rate formula, conversational-turns-per-task, task normalization, and a new "Safety mechanism validation" item for confirmation-gate/undo-log correctness checks — all previously described only in chapter prose, untracked as things to actually build/test. No drift found on any of today's earlier edits.

**Created `drift.md`** — tracks technical claims in this project that can go stale over time (SLM choice, NL2Bash benchmark citations, unwritten Go/Ollama/bubbletea APIs in `prototype/`, Playwright's experimental CDP features, the Debian/XFCE version target), with a "when to recheck" trigger list. Distinct from the consistency audit above — that was doc-vs-doc agreement *right now*; this is about facts becoming stale *later*.

### Session 5 — 2026-07-07

- **Corrected a chapter-numbering error: the Methodology submission (SA3.1) was mislabeled "Chapter 2" and is now "Chapter 3."** Surfaced by the Blackboard title of the module 3 reference deck ("Chapter 3"). Verified against `module 3/references/writing-guidelines.pdf` (Mapúa TOC: Ch.1 Introduction, **Ch.2 Review of Related Literature**, **Ch.3 = major aspect / journal-format methodology**, Ch.4 Conclusion, Ch.5 Recommendation) and the Sonam reference thesis, whose methodology chapter uses `Table 3.1` / `Figure 3.1`-`3.2` numbering — confirming the methodology chapter is Chapter 3.
- **Scope of the edit (deliberately narrow):** changed only the chapter label (title, heading) and the *chapter-prefixed* table/figure numbers + anchors — `Table 2.1→3.1`, `Table 2.2→3.2`, `Figure 2.1→3.1`, `table-2-*`/`figure-2-1` ids. **Left untouched:** the in-article Section structure (Section 1 Dataset / Section 2 Procedure / Section 3 Testing) and its `1.x`/`2.x`/`3.x` subsection numbers, `sec2-*` anchors, and the `Section 2.3` / `Section 2.1a` cross-references — these are section numbers, *not* chapter numbers, and a blind 2→3 replace would have collided the Section 2 subsections with the existing Section 3 ones. The `Chapter 1` back-reference (Introduction) was correctly preserved. Re-exported the PDF.

### Session 6 — 2026-07-08

- **Added the official `proposal-grading-rubrics.pdf`** to `module 3/references/` (transferred from local VM); it confirms the chapter structure (Ch.2 = RRL, Ch.3 = Methodology) and supplied the RRL grading criteria. Also transferred `GROUP5_SA2.1_ResearchMethods.odt` (RRL context) and a `module 1` course folder; **tidied** the latter out of `module 3/references/` into a new sibling `research-methods/module 1/` split into `references/` (course examples/samples) and `submissions/` (own work).
- **Drafted Chapter 2 (Review of Related Literature)** — new `module 3/submissions/summative-assessment-2.html` + PDF (26 pp.). Reviewed `module 1/submissions/Soriano_SA1.1_ResearchMethods.pdf` first (a 20-paper survey, the reference-dense source). **Key finding:** SA1.1's bibliography aligns 1:1 with the current thesis numbering, and the "gap" numbers [4],[6],[13],[14],[19],[20] are exactly the six survey papers SA1 didn't cite (van Dam, DexAssist, CogAgent, ScreenAgent, PixelHelp, ReAct) — so Ch.2 reuses them at their reserved numbers with **zero renumbering**. Chapter cites 24 real papers ([1]–[20],[22]–[25]); citation integrity verified (every cite defined, every ref cited). Deliberately excluded SA1.1's **outdated** proposed architecture (multimodal MCP + AT-SPI + vision three-tier); reframed the gap/positioning to the current CLI/NL-to-bash + Qwen2.5-Coder-3B design, and honored the vision/business scope guard. Structure follows the rubric: §2.1 intro → §2.2–2.6 thematic research literature → §2.7 synthesis + Table 2.1 matrix + threefold gap → §2.8 methodological literature → §2.9 summary/transition to Ch.3.

### Session 7 — 2026-07-08 (Ch.2 alignment audit + patch)

- **Audited the Ch.2 draft against Ch.1, Ch.3, the rubric, and the writing guidelines** (not just SA1.1). Confirmed strong substantive alignment — identical threefold gap, consistent system framing, reference numbering [1]–[20],[22]–[25] consistent across all three chapters, and the §2.8→Ch.3 transition promises methods (within-subjects, expert-baseline, counterbalancing, think-aloud, thematic analysis [24], confirmation-gate/undo) that all actually appear in Ch.3 — so **not** a blind survey copy. Surfaced five gaps and patched all of them.
- **Added §2.2 Theoretical Framework** (the four-layer Input→Reasoning→Action→OS-Integration pipeline, adapted from [18]) as a distinct numbered section — the rubric grades a "Theoretical Framework" component under Ch.2 (weight 5) that the draft was missing. Sections **renumbered §2.2–2.9 → §2.3–2.10** (headings, ids, TOC, and 5 in-text `§2.x` cross-refs remapped; citation integrity re-verified, TOC count == heading count == 10). Reconciled with Ch.3: Ch.3's "Conceptual Framework" (Figure 3.1) is the concrete **SynapseOS system architecture**, so the pipeline is Ch.2's complementary *theoretical* lens, explicitly bridged ("Chapter 3 instantiates this pipeline").
- **Added Figure 2.1** — a self-contained inline-SVG diagram of the four-layer pipeline (resolves the previously-deferred optional figure); added a **List of Figures** front-matter block. Renders as vector in the PDF; PDF outline confirmed §2.1–§2.10 + the six new synthesis subheadings + Notes/References.
- **Promoted the six §2.8 synthesis patterns to `h3` subheadings** (rubric's exemplary band wants "themes evident in headings").
- **Reconciled the "convergent mixed-methods" label** — §2.9 (Ch.2) asserted it but Ch.3 never named the design; added one framing sentence to **Ch.3 §1.4** naming it a convergent (parallel) mixed-methods design (also strengthens Ch.3's "type of study" rubric row). Both PDFs re-exported (Ch.2 now **29 pp.**; page numbers re-derived via `find_page()`).
- **Documented two standing choices in `decisions.md`:** D16 (numbered ACM-style citations with DOIs/URLs thesis-wide — a deliberate override of the ITRD handout's "no internet references" note, per the rubric's ACM directive) and D17 (shared master bibliography; `[21]` intentionally reserved/unused to avoid desyncing submitted chapters).

### Session 8 — 2026-07-08 (shipped to `main`)

- **Landed the entire Session 5–7 body of work on `main` via four squash-merged PRs** (`/ship`), partitioned by concern: **#3** `docs(refs)` course reference materials (Module 1 folder, GROUP5 SA2.1, grading rubric) · **#5** `fix(ch3)` methodology-chapter alignment (Ch.2→3 reference correction + the convergent-mixed-methods sentence) · **#6** `docs` decisions D16–D17 + tracking refresh · **#4** `feat(ch2)` the Chapter 2 RRL chapter (tip of `main`). Working tree clean, local `main` fast-forwarded to `2fc7f9c`.
- **Process note:** `Table 2.x`→`3.x` renumber and the mixed-methods sentence both live in `summative-assessment-3.1.html`, so PR #5 bundles them (file-granularity, can't hunk-split). Auto-merge enqueue was blocked by a review guardrail; you merged the PRs manually.

### Session 9 — 2026-07-08 (cross-chapter/vision/rubric audit + patch)

- **You confirmed Ch.2's content read** — §2.2 and the Ch.3 §1.4 mixed-methods sentence read as yours. No further action on that item.
- **Ran three parallel audits** (per your request to check Ch.2↔Ch.1/Ch.3 alignment and thesis-wide vision/guideline alignment) covering cross-chapter consistency, vision/scope alignment, and formal guidelines/rubric compliance (the last one closed a real coverage gap — Ch.1 and Ch.3 had never been checked against the official rubric PDF, only Ch.2 had). Vision alignment came back fully clean. The other two surfaced real, verified gaps, all patched:
  - **Ch.3 citation integrity was broken**, contradicting D17: `[3]` NaSh, `[5]` VoicePilot, `[16]` UFO², `[22]` GUIRoboTron-Speech, `[23]` R-VLM were listed in SA3.1's reference list but never cited in its body. Investigated each: `[3]`/`[5]` are genuinely relevant to the confirmation-gate/undo-log design (Ch.2 already names them as its motivation) and are now cited in §2.1b; `[16]` (UFO²) is now cited in the same section's GUI-scope-boundary sentence as a deliberate contrast; `[22]`/`[23]` have no legitimate connection to Ch.3's CLI-only methodology and were removed from its reference list — both remain correctly cited in Ch.2, where the underlying GUI/speech-grounding discussion actually lives.
  - **Ch.3's introduction never restated the three RQs from Ch.1** — the rubric's "Introduction" row requires problem *and* question. Added a paragraph restating RQ1–RQ3 and mapping each to the section that answers it.
  - **Ch.3 had no Appendices section** for the SUS/NASA-TLX instrument copies the rubric grades. Added one with Appendix A (SUS) / Appendix B (NASA-TLX) placeholders, pending the finalized instruments from the Study Instruments track.
  - **Minor pre-existing formatting deviations** (h3 heading case, page-number position, Ch.1's heading-based Introduction structure) were reviewed and deliberately left as-is — logged in `scope.md`'s Final Thesis Compilation section as a documented judgment call, not pursued further.
  - **Ch.2's figure-caption font-size** (11pt) never matched Ch.3's established 12pt convention (Session 4) — fixed for consistency.
- **Resolved the recruitment quota decision** (open since Session 4): added **D18** — minimum 2 participants per primary-OS background (Windows/macOS/Linux) within the n=20 sample. Reflected in `decisions.md`, `scope.md`'s recruitment item, and a new sentence in SA3.1 §1.2b.
- **Both PDFs re-exported**; Ch.3's TOC/LOT/LOF page numbers fully re-derived via `find_page()` against the new content (now 35 pages, up from 29) and spot-verified stable across the final two re-exports; Ch.2's page numbers reverified unchanged (29 pages) after the caption-font fix.

### Session 10 — 2026-07-08 (consolidated proposal audit + patch)

- **You asked whether the consolidated Chapters 1–3 monograph (`SynapseOS_Proposal_Chapters_1_to_3.html/pdf`) was submission-ready.** It was not — the file predates this session's state.md history (never logged, apparently built and then hand-edited outside of a tracked session) and had drifted out of sync with its own generator, `tools/consolidate_proposal.py`.
- **Root cause:** the HTML had Approval Page / Acknowledgement / Abstract front matter hand-inserted directly into the output, but the script that regenerates this file has no knowledge of that content and was never updated or rerun afterward. Three concrete defects followed:
  1. **Stale TOC/LOT/LOF page numbers** — every entry from Chapter 1 onward pointed 3–5 pages too early because the front-matter insertion was never accounted for.
  2. **Appendices ordered before References** — Chapter 3's own Appendices section (added Session 9) landed in the document body ahead of the consolidated master References block, contradicting both the guideline's sample TOC order and the TOC's own listed order.
  3. **Three orphaned `<div class="references">` wrapper tags** (one per chapter) — `strip_references_section()`'s no-next-heading fallback truncated at the `<h2>` start instead of the wrapper's own closing tag, leaving Ch.2 and Ch.3's entire remaining content nested inside an unclosed div from the previous chapter.
- **Fixed all three at the source**, not by hand-patching the output: folded front-matter generation into `consolidate_proposal.py` (front matter is now templated, so `Approval → Acknowledgement → TOC → LOT → LOF → Abstract → Chapter 1` order matches the writing-guidelines Section F sample); added `extract_appendices()` to pull Chapter 3's Appendices block out before reference-stripping and re-attach it after the master References section; rewrote `strip_references_section()` to consume the full `<div class="references">…</div>` wrapper (including its nested `ref-entry` divs) as one balanced match instead of truncating mid-structure.
- **Extended the two-pass page-detection** (`detect_page_numbers()`) to cover the new front-matter headings (APR/ACK/ABS) in addition to the existing TOC/LOT/LOF/CH1/CH2/CH3/REF/APP targets, so all of these stay self-correcting on rebuild.
- **Verified exhaustively against the rebuilt PDF**: every top-level and sub-level TOC/LOT/LOF entry checked against actual `pdftotext` page locations (found and fixed two more pre-existing off-by-one errors unrelated to the front-matter bug: `2.8 Synthesis`/`Table 2.1` and `Section 3: System Testing` were each one page early — these use hardcoded chapter-relative offsets, not per-heading detection, and had drifted from earlier content edits); confirmed all 24 references ([1]–[20],[22]–[25]) are cited-and-listed with no orphans on either side; confirmed div-tag balance (was 62 open / 59 close, now 59/59) with byte-identical rendered text before and after the structural fix.
- **Known limitation, not fixed:** front matter (Title/Approval/Acknowledgement/TOC/LOT/LOF/Abstract) uses plain continuous Arabic page numbers, not the roman-numeral-then-restart-at-1 scheme the guideline's illustrative sample shows — Chromium's print engine (used by `tools/export_pdf.py`) has no native support for resetting its page counter mid-document, and the already-graded individual chapter submissions use continuous Arabic too, so this was kept consistent with existing project precedent rather than engineering a workaround. Flagged for you to weigh in on if it matters for grading.
- **Result:** consolidated proposal PDF is now internally consistent — every TOC/LOT/LOF page number matches actual content, section order matches the guideline sample, and the HTML is structurally valid. *(Correction, Session 11: this was actually committed shortly after this session, in `5cb27f8`/`030f2b0` — the "not committed" note above was stale by the time anyone read it again.)*

### Session 11 — 2026-07-10 (docs reorg, consolidated-proposal ship, Paged.js pipeline)

- **Reorganized top-level planning docs into `docs/`** — `brainstorm.md`, `decisions.md`, `drift.md`, `future-features.md`, `gui-tui-implementation.md`, `layers.md`, `roadmap.md`, `scope.md`, `stack.md`, `state.md`, `vision.md`, `wordbank.md` all moved from the repo root into `docs/`, alongside `research-methods/` and `tools/`. Pure rename, no content change.
- **This file renamed `state.md` → `session.md`** — same document, same purpose (session-to-session handoff); the old name undersold what it actually does.
- **Picked up a restructure that a prior session had left mid-flight, undocumented**: `tools/consolidate_proposal.py` existed on disk and had been generating the committed consolidated-proposal output since `5cb27f8`, but the script itself had never been added to git — an oversight, not a decision. Working tree also had the old flat output deleted (uncommitted), a new `research-methods/consolidated/` directory with relocated + regenerated output, and two new untracked scripts (`consolidate_proposal_paged.py`, `export_pdf_paged.py`) plus a vendored `paged.polyfill.js` — a Paged.js-based build path for CSS Paged Media features Chromium's print engine lacks natively.
- **Caught a regression before it shipped**: both new generator scripts had "Department of Information Technology" / "Bachelor of Science in Information Technology" hardcoded in their front-matter template — silently reverting the department/degree fix from the immediately-preceding commit (`030f2b0`, Computer Science). Fixed both scripts at the source and regenerated all four affected outputs before staging anything. Confirmed the non-paged relocated HTML is now byte-identical to what was on `main`, aside from the directory move.
- **What the Paged.js pipeline actually does — for the record, since nothing documented it**: adds page-bottom footnotes (`float: footnote` in CSS, which Chromium's print-to-PDF has no equivalent for) as a parallel output alongside the existing flat build. It does **not** implement the roman-numeral-then-restart front-matter pagination flagged as a known limitation in Session 10 — that page-numbering CSS was never added to the Paged.js script either. Still open; see Pending Items.
- **Shipped via `/ship`** (2 PRs, both merged): **#8** `docs: relocate planning docs into docs/ directory` (+ the Session 10 log entry) · **#9** `build(proposal): relocate consolidated proposal build and add Paged.js pipeline`. Originally planned as 3 islands, but cherry-picking the Paged.js commit onto bare `main` failed — it depends on the `research-methods/consolidated/` directory the relocation commit creates — so it was folded into the same branch/PR. This repo has no CI and `allow_auto_merge` is off at the repo level; both PRs merged immediately since nothing was pending.

### Session 12 — 2026-07-10 (CLI mode + GUI fallback: D19/D20)

- **Fixed three real contradictions surfaced by a pre-work audit**, all committed separately from the feature work below: (1) M2 was marked "✅ done" in three places (`build-order.md`, `prototype/README.md`, `roadmap.md`) despite `session.md`'s own Pending Items saying it had never been run against live Ollama — downgraded to "🚧 code complete, live validation pending" everywhere, matching `build-order.md`'s own "done means testable" rule; (2) the Session 11 docs reorg broke every `../*.md` cross-reference inside `prototype/` (`decisions.md`, `scope.md`, `stack.md`, `vision.md`, `roadmap.md` all moved to `docs/`) — repointed to `../docs/...`; (3) `prototype/README.md` cited "D1–D11," now well behind D18.
- **Decided, after discussion, to formalize CLI mode as a third interface mode** (D19) — the M2 walking skeleton's one-shot invocation pattern, promoted from disposable TUI scaffolding to a permanent shipped mode (scripting/automation/remote one-off use), alongside TUI and GUI.
- **Decided to add a participant-accessible GUI-mode fallback to XFCE** (D20, amends D12) — you chose participant-accessible over the facilitator-only-hidden trigger initially recommended, specifically for safety ("just in case things go south"), not casual mid-task switching. Since D12's original no-escape-hatch design existed specifically to protect the within-subjects comparison (D5), the mitigation is in the data handling, not access restriction: every fallback invocation is logged (participant ID, task ID, timestamp), any task where it's invoked is excluded from the primary completion-time/error-rate analysis (scored as "did not complete via SynapseOS," not a silent success), and fallback-invocation rate is reported as its own secondary metric.
- **Archived the pre-revision submitted state first** (Phase 0, since SA1/SA2/SA3.1 were treated as graded coursework at the time) — `research-methods/archive/submitted-2026-07-10/` holds byte-identical copies of all three chapters plus both consolidated-proposal variants, verified against a `submitted-2026-07-10` git tag; both the tag-vs-archive match and the archive-vs-live divergence were confirmed before closing this session. *(Correction, Session 13: SA2/SA3.1 were never actually submitted individually — only SA1 and the consolidated document were. The archive is still valuable, just not for the reason originally stated; see Session 13.)*
- **Propagated D19/D20 across every planning doc** that named TUI/GUI as the only two modes: `layers.md`'s mode table (now four rows) and FAQ, `scope.md`'s deliverables and Final Thesis Compilation tracker, `build-order.md`'s M2 and M9 (plus a stale "Wayland desktop" M9 done-when criterion corrected to XFCE X11 session per D12, caught while already in that section), `prototype/README.md`, `vision.md`'s Horizons table, `stack.md`'s runtime environment table, `roadmap.md`'s H0 line, and the root `README.md` tagline (also applying the userland→session-layer fix agreed earlier this session, paused pending this discussion).
- **Updated Ch.3 (SA3.1) itself** — the actual graded content change: Table 3.1 gets a fallback note, Table 3.2 and its following paragraph add CLI mode, Section 3.5 gets a new "Fallback reliance" Internal Validity entry (threat + mitigation, matching the existing Learning effect/Demand characteristics/Fatigue entries' style), footnote 8 defines CLI mode. Chapter grew 35 → 38 pages; TOC/LOT/LOF re-derived via `find_page()` against distinctive body-text snippets (bare heading-title search collided with the TOC's own text, returning false early-page matches — anchored `id="..."` occurs exactly once per heading, `href="#..."` doesn't, so body-text-snippet search was the reliable method) and spot-verified against the rebuilt PDF.
- **The predicted drift from Session 11's Pending Items materialized and was fixed**: Ch.3's content growth desynced three hardcoded chapter-relative TOC offsets in `consolidate_proposal.py` (Table 3.2, Figure 3.1, Section 3) — caught by re-verifying against the rebuilt consolidated PDF rather than trusting the two-pass build's self-correction (which only covers the top-level chapter/front-matter starts, not sub-entries within a chapter). The Paged.js variant needed **entirely different offset values**, not the same ones — page-bottom footnotes consume enough extra vertical space that Section 3's offset differs by 10 pages between the two variants (`p_ch3 + 11` flat vs. `p_ch3 + 21` paged). Both rebuilt and independently verified.
- **Citation/footnote integrity re-confirmed**: SA3.1's footnote references (1–8) exactly match its footnote definitions; reference-citation `[N]` usage untouched by this session's edits.

### Session 13 — 2026-07-10 (retire standalone Ch.2/Ch.3 drafts)

- **You corrected a wrong assumption from Session 12**: only Chapter 1 (SA1) and the consolidated Ch.1–3 monograph were ever actually submitted through the course portal. The standalone `summative-assessment-2.html`/`-3.1.html` files (Ch.2 RRL, Ch.3 Methodology) were drafts that fed the consolidated document, never themselves submitted — the "receipt.txt" in `module 2/submissions/` that looked like confirmation turned out to be from an unrelated course entirely ("Artificial Intelligence 4," not Research Methods), so there was never real evidence either way; you supplied the actual fact.
- **Decided to retire the per-chapter split for Ch.2/Ch.3 entirely** rather than just rename/relocate the drafts — maintaining both the standalone files and the consolidated document risked drift between them, and the standalone files were never load-bearing for grading in the first place. Deleted `research-methods/module 3/submissions/summative-assessment-2.{html,pdf}` and `-3.1.{html,pdf}`; the consolidated document is now hand-maintained directly rather than generated from separate chapter files.
- **Retired `tools/consolidate_proposal.py` and `tools/consolidate_proposal_paged.py`** — both read the now-deleted files as their build input, so they had nothing left to consolidate from. `tools/export_pdf.py` and `export_pdf_paged.py` (the exporters, not generators) are unaffected and still used to render the consolidated HTML directly.
- **Corrected the Session 12 archive's framing** rather than deleting it — `research-methods/archive/submitted-2026-07-10/` still holds the last copies of the SA2/SA3.1 drafts anywhere (now that the live ones are gone) plus the true submissions (SA1, consolidated), but its README no longer mischaracterizes the drafts as "submitted, graded coursework."
- **Added `research-methods/module 3/submissions/README.md`** explaining why that directory is now empty, since a bare empty folder with no explanation would just reopen the same confusion later.
- **This was flagged, not assumed, as the reason to defer the footnote-rendering fix**: fixing page-bottom footnotes for Ch.2/Ch.3 content now only touches two targets (Chapter 1 standalone, and the consolidated document) instead of four — simpler than the plan discussed earlier this session, since two of the original four targets no longer exist.

### Session 14 — 2026-07-10 (retire Ch.1 + flat consolidated; footnote rendering resolved)

- **Verified, before claiming anything was fixed, exactly which documents actually had working page-bottom footnotes.** Checked `pdftotext -layout` output on the paged consolidated PDF directly: footnote markers 8/9 (CLI mode, TUI mode) in Table 3.2 and their definitions both land on page 43 — genuine same-page rendering, not just collected-somewhere-in-the-middle endnotes. Confirmed Chapter 1 standalone and the flat consolidated variant both still had `class="footnotes"` endnote blocks, unfixed.
- **You extended Session 13's reasoning one step further**: since the flat consolidated variant is the only place that still renders footnotes wrong, and Chapter 1's standalone HTML is the same kind of drift risk the Ch.2/Ch.3 drafts were, retire both down to their already-submitted frozen PDFs — same pattern, same justification.
- **Retired Chapter 1's standalone HTML** (`module 2/submissions/summative-assessment-1.html`) — the PDF (already submitted) stays as the permanent record; no live source remains for this chapter. Any future Ch.1 content changes now happen in the consolidated document instead.
- **Retired the flat consolidated variant's HTML** (`consolidated/SynapseOS_Proposal_Chapters_1_to_3.html`) — its PDF already carries the Session 12 D19/D20 content (rebuilt before the generator was deleted in Session 13), so it's a legitimate, up-to-date frozen snapshot, not a stale one. The flat variant is no longer maintained at all.
- **Net effect: footnote rendering is resolved by elimination, not by extending the Paged.js fix to more documents.** The paged consolidated document is now the *only* live document in the repo, and it already renders footnotes correctly (has since Session 11). There is nothing left with broken footnote rendering to fix.
- **Fixed the root README's directory tree** — it listed a `summative-assessment-1.txt` that never existed in that form (same class of error as the Ch.3 `.txt` reference fixed in Session 13), and still listed the now-deleted `.html` files for both Ch.1 and the flat consolidated variant.

---

### Session 15 — 2026-07-12 (font-resolution bug fixed; anchor collisions and orphaned headings resolved)

- **Root-caused the "formatting looks broken" report**: the entire document was rendering in DejaVu Sans instead of Times New Roman/Liberation Serif, in every PDF export produced by this pipeline's history, not just this session's. `FONTCONFIG_PATH` in both `tools/export_pdf.py` and `tools/export_pdf_paged.py` pointed at `tools/vendor/fonts` — a bare folder of `.ttf` files with **no `fonts.conf`**. Confirmed via a vendored `fc-match` that fontconfig silently fails to load any config in that state and falls back to an arbitrary font, ignoring every `font-family` rule in the source CSS — the document's own CSS was correct the whole time.
- **Fixed by generating `fonts.conf` dynamically at export time** (`write_fontconfig()` in both scripts, called at the start of `export()`) rather than committing a static file — the `<dir>` path is baked in as an absolute path freshly computed from `Path(__file__).resolve()`, so it can't go stale or break if the export is invoked from a different working directory. A relative `<dir>` was tried first and rejected: fontconfig resolves it against the *invoking process's* cwd, not the config file's own location, so it silently produced zero font matches when tested from outside the repo root.
- **Consequence: pagination shifted hard** — Liberation Serif's metrics differ substantially from the wrong fallback font, so every one of the document's 36 TOC/LOT/LOF entries went stale in one move (73 → 61 pages on the first re-export). Re-derived all 36 via `find_page()` against distinctive body-text snippets (not bare heading titles, which collide with the TOC's own listing text) — full round-trip re-verified clean against the final PDF.
- **Found and fixed a real, pre-existing HTML defect while building those snippets**: 21 duplicate `id` attributes. Chapter 3's own internal subsection numbering ("Section 1" → 1.1, 1.2…; "Section 2" → 2.1, 2.2…) reused the same unprefixed `secN-M` id pattern that Chapter 1 and Chapter 2 already used for their own, unrelated subsections, plus all three chapters' footnote "Notes" headings shared `id="sec-notes"` (Ch.3's had no id at all). Namespaced all of Chapter 3's internal ids with a `ch3-` prefix and the three Notes headings as `ch1-notes`/`ch2-notes`/`ch3-notes`, matching the existing per-chapter footnote-namespacing precedent (`ch1-fn1`, etc.). Confirmed via grep that nothing outside Chapter 3 ever linked to its own colliding ids, so this was a clean rename with no dangling `href`s.
- **Found and fixed 3 orphaned headings** ("Conceptual Framework," Ch.3's "Section 2: Procedure / Experiment," "3.4 Data Analysis Plan") stranded alone at the bottom of a page with their body text pushed to the next page. Tried `break-after`/`page-break-after: avoid` on `h2`/`h3` first — traced through `paged.polyfill.js` and confirmed this vendored Paged.js version never actually implements `break-after: avoid` as a real constraint (it only excludes "avoid" from the small set of *forcing* values it does support: always/page/left/right/recto/verso). Removed that dead CSS rather than leave it in looking functional. The working fix: wrap each affected heading with its first paragraph in a `<div style="break-inside: avoid;">`, which the polyfill does actively enforce — verified visually via rendered PNG that all three now land on the same page as their body text.
- **Full re-verification pass, all clean**: no duplicate `id`s remain anywhere in the document; a document-wide orphan scan found no further instances beyond the 3 fixed; citation integrity check found no orphaned or unused `[N]` markers (one pre-existing gap noted — reference `[21]` is absent from the list but also never cited, so nothing is actually broken, left alone as out of scope for this session); all 36 TOC/LOT/LOF entries match their PDF page numbers exactly on the final 62-page export.
- Cleaned up a stray `cache/` directory at the repo root — fontconfig cache debris from ad-hoc `fc-match` diagnostic runs earlier in the session, before `fonts.conf` had a `cachedir` configured. Not part of the repo.

---

### Session 16 — 2026-07-12 (Paged.js retired; endnotes/flat variant is the living document again)

- **You asked why the paged variant had ballooned to 62 pages when the true submitted PDF was 42.** Measured it directly rather than guessing: word counts between the two are within 65 words of each other (14,751 vs 14,816) — the gap is not content, it's rendering. Restored the true submitted flat/paged HTML+PDF pairs from `research-methods/archive/submitted-2026-07-10/` into `research-methods/consolidated/` as side-by-side reference copies to make this checkable directly rather than taking it on faith.
- **Root-caused the page-density gap.** Only 14 of 62 paged pages carry a footnote, so genuine footnote-space overhead doesn't explain most of the growth. Measured words-per-page: ~257 (paged) vs. ~392–402 (flat) — a ~35% density drop present even on completely footnote-free pages. Visually confirmed why: some pages (p.18) pack edge-to-edge exactly like the flat variant; others (p.16) end with 1.5–2 inches of dead blank space with no footnote anywhere nearby. This is the vendored Paged.js polyfill's own page-break estimation being imprecise at certain fragmentation points — a real limitation of JS-driven reflow versus native browser layout, not a footnote cost.
- **Verified current browser support for `float: footnote`** (MDN, checked live rather than trusting training data since browser feature support drifts): still zero native support in Chrome, Firefox, or Safari. So the choice was never "Paged.js vs. plain CSS" — same-page footnotes require a JS polyfill today, period. The real choice was "accept the polyfill's page-count and fragility cost, or go back to endnotes."
- **You conceded the same-page-footnote goal** given that cost. Endnotes-per-chapter are a normal, accepted academic convention, not a downgrade — decided to make the flat variant the living document again and retire the Paged.js pipeline as an artifact rather than delete it (same archive-not-delete pattern as Session 13/14).
- **Restored `SynapseOS_Proposal_Chapters_1_to_3.html`** from git history (the exact state right before Session 14 deleted it, commit `bedd839^`) — already had the D19/D20 content, so nothing was lost. It had the **same** duplicate-`id` bug found and fixed in the paged variant in Session 15 (Chapter 3's internal subsection ids colliding with Chapter 1/2's, plus the three chapters' `Notes` headings colliding) — applied the identical `ch3-`/`ch1-notes`/`ch2-notes`/`ch3-notes` namespacing fix here too, plus fixed one stale `href="#sec-notes"` TOC link the rename would have orphaned.
- **Found 2 genuinely orphaned headings here too** ("Gap / Opportunity," "2.3 Historical Foundations of Natural-Language Interfaces") — same defect class as Session 15, but this time `page-break-after: avoid` on `h2`/`h3` worked correctly on the first try, since this pipeline uses native Chromium print-to-PDF rather than a JS polyfill. No `break-inside` workaround needed here.
- **Re-derived all 37 TOC/LOT/LOF entries** against the resulting 43-page PDF (was 42 with the pre-Session-15 font bug also present in this file, now fixed here too), full round-trip verified clean, no duplicate ids, no orphans anywhere in the document.
- **Retired the Paged.js pipeline to `research-methods/archive/paged-footnotes-attempt/`**: `SynapseOS_Proposal_Chapters_1_to_3_paged.html`/`.pdf`, `paged.polyfill.js`, and `tools/export_pdf_paged.py`, with a README covering what's there, the full reasoning above, and how to revive it if same-page footnotes are worth revisiting later (e.g. for a final thesis copy, or if a browser ever ships native `float: footnote` support).

---

### Session 17 — 2026-07-12 (prototype: M2 validated live; CLI mode redefined to include execution + confirmation gate)

- **Ran the M2 walking skeleton against a live Ollama instance for the first time** — the empirical risk flagged as open since the milestone was written. Ollama was already running (a prior session had left it up) but only had `llama3.2:3b` pulled, not the `qwen2.5-coder:3b` the prototype targets (D3); the `ollama` CLI binary itself wasn't reachable from the shell despite its server process running, so pulled the correct model directly via the HTTP API instead (`POST /api/pull`). Once pulled, all 8 sample-suite tasks produced a proposed command in 1.2–10s — model viability at the 3B budget, the single largest empirical unknown in the runtime, is retired.
- **That run surfaced two real findings, not hypothetical ones**: (1) `cleanCommand` only strips triple-backtick fences, not inline single backticks — 3 of 8 outputs came back wrapped in single backticks, which would hand a broken, non-executable string to the eventual execution engine as-is; needs fixing before execution lands. (2) At least one command was plausible but semantically wrong (`dpkg-query -Wf` piped through a `grep` pattern that only matches `dpkg -l`'s different output format) — a model-accuracy limitation, not a code bug, and exactly the kind of failure the study's evaluation methodology is designed to catch.
- **You redirected the build order**: rather than proceeding straight to M3 (TUI) per the original sequence, make CLI mode (M2) a genuinely complete, useful interface first — propose, classify reversibility, and execute, mirroring how Claude Code's own auto-mode permission model works (auto-run what's judged safe, stop and confirm what isn't). Reasoning: TUI can't meaningfully be built as "just a wrapper" until there's a working core to wrap, and execution/confirmation don't actually need a TUI to exist — that dependency in the original build order was a sequencing choice, not a technical requirement.
- **Restructured `prototype/build-order.md`**: M2's scope now explicitly includes execution and the reversibility classifier (previously scoped as standalone M4/M5, depending on M3 existing first). M4 and M5 are marked merged into M2 rather than deleted, matching how superseded decisions in `decisions.md` are handled (kept for milestone-number continuity, not renumbered — avoids breaking cross-references elsewhere). M3's dependency updated to require M2 fully complete, not just the walking-skeleton shape. M6/M7/M8's dependency lines updated to point at M2 instead of the now-absorbed M4/M5; M6 also gained an explicit M3 dependency, since multi-turn session context can't exist in a one-shot CLI regardless of what M2 delivers.
- **Propagated the same change to `prototype/README.md`** (status table), **`docs/roadmap.md`** (H0 status line), and **`docs/scope.md`** (CLI mode, Bash execution engine, and Confirmation gate deliverable entries) — same pattern used throughout this project for keeping planning docs in sync after a scope decision.
- Explicitly scoped auto-execution to the ad-hoc single-task path (`synapse "<task>"`) only — the built-in 8-task sample suite stays propose-only, so running it for a smoke test never actually installs packages, moves files, or touches the real filesystem.
- **You asked whether the paper itself needed updating to match the M2 redefinition — it did.** Ch.3's Table 3.2 paragraph and footnote 8 both described CLI mode as propose-only ("translates a request into a proposed command and exits"), now stale against today's decision to make CLI mode execute through the same confirmation gate as the other modes. Fixed both passages to reference the existing Execution Pipeline description (Section 2.1) rather than reinventing the framing. No pagination shift; all 37 TOC/LOT/LOF entries re-verified against the rebuilt PDF, unchanged.
- **Also corrected a claim from earlier this session**: checked the *actual* originally-submitted PDF's embedded fonts (`pdffonts`, not just eyeballing a render) and confirmed it has the same DejaVu Sans bug fixed in Session 15/16 — the graded submission was never Times New Roman either. The current living document is now *more* writing-guideline-compliant than what was actually submitted, not a deviation from a "more accurate" original.

### Session 18 — 2026-07-12 (prototype: M2 completed — reversibility classifier and execution wired up)

- **Fixed the `cleanCommand` single-backtick bug found in Session 17.** It previously only stripped triple-backtick fences; now also strips a whole response wrapped in a single pair of inline backticks (the shape 3 of 8 live outputs actually came back in). Both cases unit-tested (`cmd/synapse/main_test.go`).
- **Built `internal/classifier`**: pattern-matches a proposed command against a short, explicit list of known-irreversible shapes — `rm`, `dd`, `mkfs`, `shred`, `git reset --hard`, `git clean -f`, and any truncating `>` redirect (append `>>` is exempted via a character scan, since Go's RE2 regexp engine has no lookbehind to express "not followed/preceded by another `>`"). Deliberately a pattern matcher, not a shell parser, matching the scope build-order.md already committed to. Table-driven unit tests cover both matches and near-miss false positives (e.g. "confirm the alarm" doesn't trigger on `rm`, "add a user" doesn't trigger on `dd`).
- **Built `internal/executor`**: dispatches a command via `sh -c` with `os/exec`, capturing stdout/stderr separately and surfacing the exit code (or a launch error, e.g. missing shell, kept distinct from a nonzero exit). Unit-tested for stdout capture, stderr capture, nonzero exit surfacing, and context cancellation.
- **Wired both into `cmd/synapse`'s ad-hoc path** (`synapse "<task>"`): propose → classify → auto-run if reversible, or print the command and reason and block on an explicit y/N before running if not → print output and exit with the command's own exit code. The 8-task sample suite (`synapse` with no args) deliberately stays propose-only, per the scope boundary agreed in Session 17 — it's a quality smoke test, not a live filesystem action.
- **Validated all three paths live against Ollama** (not just unit tests): a reversible task ("list the files in the current directory" → `ls`) auto-ran with output shown, no prompt; an irreversible task ("delete scratch.tmp" → `rm /tmp/scratch.tmp`) blocked, printed the reason, and correctly cancelled on empty stdin; a second irreversible task (a truncating redirect) blocked and, on `y`, executed correctly — file contents verified afterward with `cat`.
- **M2 is now marked done** (`build-order.md`, `README.md`, `roadmap.md`, `scope.md` — CLI mode, Bash execution engine, and Confirmation gate deliverable rows all flipped to `[x]`). M3 (TUI loop) is next; its "depends on M2 fully complete" gate is now satisfied.
- **Found and fixed a stale checklist entry while updating `scope.md`**: line 170 still said "D19/D20 not yet reflected in SA3.1," but that was resolved back in Session 12/`aa9fbee` (2026-07-11) — the paper already covers CLI mode and the XFCE fallback. Marked `[x]` with a corrected timeline, including the Session 17 footnote-8 refinement.

---

## Pending Items

- **NDSS LAST-X 2026 citation:** author list and full citation metadata resolved 2026-07-05 (Jacobs, Lapon, Naessens — DistriNet, KU Leuven), citable now. The per-model accuracy table inside the PDF is still unextracted (binary/FlateDecode, no available tool gets past it) — only cite as corroborating literature, not for its specific ranking. Noted in `brainstorm.md`.
- **Custom cross-platform task suite:** task list (individual prompts, expected outcomes, difficulty ratings) does not exist yet. Only categories are specified in the paper (Section 1.6).
- **Study hardware:** now a 3-machine dual-boot setup (D9, revised 2026-07-05) — exact processor/RAM/storage specs still "to be confirmed prior to pilot study" per Table 3.1.
- **TOC page-number maintenance:** hardcoded, not self-updating — re-derive via `tools/export_pdf.py`'s `find_page()` before any future edit changes content. Applies to `research-methods/consolidated/SynapseOS_Proposal_Chapters_1_to_3.html`, the sole living document as of Session 16 — there is no other live chapter source left. Full detail in `scope.md`'s Final Thesis Compilation section.
- **Consolidated proposal page numbering is fully manual, not self-correcting.** Before Session 13, `tools/consolidate_proposal.py`'s two-pass build auto-detected the chapter/front-matter *starts* (APR/ACK/TOC/LOT/LOF/ABS/CH1/CH2/CH3/REF/APP) and only left sub-entries within Chapter 2/3 as hardcoded offsets. That script is retired (Session 13) — the consolidated HTML is hand-maintained directly now, so **every** TOC/LOT/LOF entry needs manual re-derivation via `find_page()` after any future edit. Search against distinctive body-text snippets, not bare heading titles — bare titles collide with the TOC's own text and return false early-page matches.
- **Front matter still uses continuous Arabic page numbers, not the roman-numeral-then-restart scheme the writing guideline's sample TOC shows** (flagged Session 10, confirmed still open Session 11 and 16). `tools/export_pdf.py` (plain Chromium print-to-PDF, the only pipeline now in use) does not implement `counter-reset`/roman-numeral CSS. If this matters for grading, it's a real CSS fix to make in the hand-maintained HTML — Chromium supports CSS counters natively, the CSS just hasn't been written.
- **Cross-chapter terminology consistency, footnote numbering unification, and the standing vision/business scope guard** — tracked in `scope.md`'s Final Thesis Compilation section; not one-time fixes, re-check on future revisions.
- **Prototype M3 — TUI loop not yet built.** M2 (CLI mode: propose, classify, execute) is complete and validated live as of Session 18 — `internal/classifier` and `internal/executor` exist, wired into `cmd/synapse`'s ad-hoc path, all three behavior paths (auto-run, block-and-cancel, block-and-confirm) demonstrated against real Ollama output. M3 wraps that same core in a bubbletea/lipgloss interactive session rather than rebuilding it; the Ollama client also needs a streaming variant for in-session token rendering.
- **`tools/venv/` and `tools/vendor/` are gitignored, not committed** — on a fresh clone or if deleted, they need rebuilding (`make setup` handles the venv/pip part; `tools/vendor/` has no automated rebuild — see `tools/TOOLING.md`).

---

## Files to Know

| File | Purpose |
|------|---------|
| `decisions.md` | All architecture and study design decisions (D1–D20) |
| `layers.md` | OS-layer reference guide — where SynapseOS sits, TUI/GUI/Overlay modes, distro-identity criteria |
| `interface-modes.md` | CLI/TUI/GUI reference — shared core, boundaries table (lifecycle/state/rendering), what each mode reuses vs. builds fresh, GUI kiosk-takeover mechanics. Renamed and rewritten from `gui-tui-implementation.md` Session 18 to add CLI mode (D19) and fix a stale sentence conflating TUI with "the CLI shell" |
| `scope.md` | Complete deliverables checklist — everything the paper has promised that must be built; includes the Critical Path and Final Thesis Compilation sections |
| `wordbank.md` | Terms to incorporate into the paper |
| `future-features.md` | Deferred ideas (not in scope for thesis) |
| `brainstorm.md` | Working notes, citations, open questions |
| `stack.md` | Implementation stack reference |
| `vision.md` | Product vision — North Star, thesis hypothesis, wedge, horizons (why, not status) |
| `roadmap.md` | Cross-horizon status index — pointers only, no duplicated status |
| `drift.md` | Drift-prone technical claims (SLM choice, library APIs, benchmark citations) and when/how to recheck each |
| `prototype/` | Go runtime: `cmd/synapse` (CLI mode entrypoint), `internal/ollama` (REST client), `internal/classifier` (reversibility rules), `internal/executor` (`os/exec` dispatch) — M2 complete; `build-order.md` has the M2–M9 milestone sequence |
| `tools/` | Self-contained PDF export tooling — `export_pdf.py` (venv + Playwright, plain Chromium print-to-PDF; the only exporter in active use as of Session 16) takes any HTML file directly; `TOOLING.md` (what's Python vs. vendored native vs. irreducible), `requirements.txt`; paired with the repo-root `Makefile`. The `consolidate_proposal*.py` generator scripts that used to build the consolidated proposal from separate chapter files were retired in Session 13 — it's hand-maintained directly now |
| `research-methods/consolidated/` | Output directory for the consolidated Ch.1–3 monograph — `SynapseOS_Proposal_Chapters_1_to_3.html`/`.pdf`, the sole living document as of Session 16 (endnotes-per-chapter, not page-bottom footnotes) |
| `research-methods/archive/paged-footnotes-attempt/` | Frozen Paged.js same-page-footnote pipeline, retired Session 16 — `export_pdf_paged.py`, `paged.polyfill.js`, and the last live paged HTML/PDF (62 pages), with a README covering why it was retired and how to revive it |
| `research-methods/module 3/references/writing-guidelines.pdf` | Mapúa ITRD writing guidelines (applied in Session 2) |
| `research-methods/module 3/references/proposal-grading-rubrics.pdf` | Official proposal grading rubric (added 2026-07-07) — grades by chapter (Ch.1 Introduction, Ch.2 Review of Related Literature, Ch.3 Methodology); authoritative source for the RRL drafting criteria |
| `research-methods/module 3/references/llm-selection-research.txt` | Research notes on LLM selection, including the 2026-07-05 reaudit appendix |

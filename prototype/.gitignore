# Compiled binaries
/bin/
/synapse
*.exe

# Go build/test cache and coverage
*.test
*.out
coverage.*

# Local env overrides
.env
.env.local

# Generated by live-model tests (testing-plan.md Layer 3/4, -tags live) —
# regenerated every run, reviewed locally, never a committed artifact
live_classifier_report.json
layer4_report.json
